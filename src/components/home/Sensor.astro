---
import { ChartColumnBig, Target, Plug } from "lucide-react";
import { Button } from "../ui/button";
import { AspectRatio } from "../ui/aspect-ratio";
const features = [
  {
    title: "Monitoreo en tiempo real",
    description: "Seguimiento continuo de la distribución hídrica",
    Icon: ChartColumnBig,
  },
  {
    title: "Alta precisión",
    description: "Mediciones exactas para optimizar recursos",
    Icon: Target,
  },
  {
    title: "Fácil instalación",
    description: "Sistema plug-and-play con configuración simple",
    Icon: Plug,
  },
];
---

<section id="producto" class="py-20 bg-background border-b">
  <div class="container mx-auto px-4">
    <div class="flex flex-col lg:flex-row items-center gap-12">
      <div class="lg:w-1/2">
        <h2 class="text-3xl font-bold text-foreground mb-6">Sensor SIMAP</h2>
        <p class="text-lg text-muted-foreground mb-8">
          El Sensor SIMAP está diseñado para monitorear y optimizar procesos
          clave de la distribución hídrica en tiempo real.
        </p>
        <div class="grid gap-6">
          {
            features.map((feature) => (
              <div class="flex items-center gap-4">
                <div class="bg-blue-200 text-blue-500 w-10 h-10 rounded-xl flex items-center justify-center">
                  <feature.Icon />
                </div>
                <div>
                  <h3 class="font-semibold text-foreground">{feature.title}</h3>
                  <p class="text-muted-foreground">{feature.description}</p>
                </div>
              </div>
            ))
          }
        </div>
        <Button className="mt-8 flex justify-center mx-auto lg:block lg:mx-0">
          Solicita una demostración
        </Button>
      </div>
      <div class="lg:w-1/2 w-full">
        <AspectRatio
          ratio={16 / 9}
          className="p-8 bg-muted rounded-lg w-full h-full"
        >
          <img
            src="/products/sensor.jpg"
            alt="Tecnología y soporte técnico"
            class="w-full h-full rounded-lg object-cover"
          />
        </AspectRatio>
      </div>
    </div>
  </div>
</section>
