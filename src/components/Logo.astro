---

---

<a href="/" class="relative inline-block font-bold text-2xl">
  <span class="flex justify-start gap-2 items-center text-primary">
    <img src="/logo.svg" alt="SIMAPI" class="w-8 h-8 inline-block" />
    SIMAPI
  </span>
  <div class="shimmer"></div>
</a>

<!-- <style>
  .shimmer {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      90deg,
      transparent 0%,
      rgba(255, 255, 255, 0.3) 50%,
      transparent 100%
    );
    animation: shimmer 2s infinite;
  }

  @keyframes shimmer {
    from {
      transform: translateX(-100%);
    }
    to {
      transform: translateX(100%);
    }
  }
</style> -->
